import { HashLoader } from "react-spinners";

const TestStudent = () => {
	return (
		<div className="flex justify-center items-center h-screen">
			<HashLoader color="#ba8cf4" loading size={50} />
		</div>
	);
};

export default TestStudent;
